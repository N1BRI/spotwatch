import 'package:flutter/material.dart';

class SpotsList extends StatelessWidget {
const SpotsList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}