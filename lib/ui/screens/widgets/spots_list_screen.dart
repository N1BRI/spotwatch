import 'package:flutter/material.dart';

class SpotsListScreen extends StatelessWidget {
const SpotsListScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return const Center(child: Text('Spots List'));
  }
}