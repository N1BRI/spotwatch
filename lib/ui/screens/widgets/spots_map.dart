import 'package:flutter/material.dart';

class SpotsMap extends StatelessWidget {
const SpotsMap({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}