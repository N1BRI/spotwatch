import 'package:flutter/material.dart';

class SpotsMapScreen extends StatelessWidget {
const SpotsMapScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
   return const Center(child: Text('Spots Map'));
  }
}